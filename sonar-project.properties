sonar.projectKey=UserManagementApi
sonar.projectName=User Management API
sonar.sourceEncoding=UTF-8

# Scan application source only (security / vulnerabilities focus)
sonar.sources=src

sonar.exclusions=**/bin/**,**/obj/**,**/Migrations/**,**/publish/**
